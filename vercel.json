{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'bagimliliklar buildCommand icinde kuruluyor'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
